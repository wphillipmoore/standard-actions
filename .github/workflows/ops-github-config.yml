name: Ops — GitHub Config

on:
  workflow_call:

permissions:
  contents: read

jobs:
  github-config:
    name: github-config
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install standard-tooling
        uses: wphillipmoore/standard-actions/actions/setup/standard-tooling@v1.5.19

      - name: Audit GitHub configuration
        env:
          GH_TOKEN: ${{ github.token }}
        run: st-github-config audit --repo "${{ github.repository }}"
