name: Documentation

on:
  push:
    branches: [develop, main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs
  cancel-in-progress: false

jobs:
  deploy:
    name: "deploy: docs"
    runs-on: ubuntu-latest
    container: ghcr.io/wphillipmoore/dev-base:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Deploy docs
        uses: wphillipmoore/standard-actions/actions/docs-deploy@v1.5.7
        with:
          version-command: cat VERSION | cut -d. -f1,2
