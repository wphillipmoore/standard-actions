name: CI Release

on:
  workflow_call:
    inputs:
      language:
        type: string
        required: true
      run-release:
        type: boolean
        default: true

jobs:
  version-bump:
    name: version-bump
    if: ${{ inputs.run-release }}
    runs-on: ubuntu-latest
    container: ghcr.io/wphillipmoore/dev-base:latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install standard-tooling
        uses: ./actions/setup/standard-tooling

      - name: Verify version bump
        uses: wphillipmoore/standard-actions/actions/release-gates/version-divergence@v1.5.3
        with:
          head-version-command: st-version show
          main-version-command: st-version show --ref origin/main
