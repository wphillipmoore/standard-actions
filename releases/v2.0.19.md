
# Release 2.0.19 (2026-05-26)

## CI

- **migrate from git hook to Claude Code hook guard**
Replace .githooks/pre-commit with .claude/hooks/guard.sh and PreToolUse hook wiring. Remove deny rules from settings.json. Update CLAUDE.md to remove git hooks setup. Ref vergil-tooling#1142

- **add SARIF warning-level enforcement**
Semgrep writes findings to SARIF and uploads to GHAS, but GHAS only fails on error-level alerts by default. Add a post-scan step that reads the SARIF output and exits non-zero on warning-or-above findings, matching the enforcement pattern from PR #564 (CodeQL).

Trivy already gates via its exit-code input on the SARIF conversion step, so no change is needed there.


## Chores

- **align shell command policy with upstream template**
The repo config audit checks that CLAUDE.md contains the canonical template as a verbatim substring. The shell command policy paragraph had diverged in wording and line wrapping. Align with the current vergil-tooling template to pass the local.claude_md audit check.


## Documentation

- **address self-review findings in container-suffix spec**
Clarify that no runtime validation is added (Approach B not C). Document fail-fast contract for ci-audit/ci-test when called without language.

- **apply pushback findings to container-suffix spec**
Add specific validate-inputs action changes for empty language handling. Note that versions stays required for interface consistency.

- **add container-suffix defaults implementation plan**
Eight-task plan covering Phase 1: workflow changes in vergil-actions. Phase 2 (consuming repo sweep) is out of scope. Ref #560
