
# Release 1.5.15 (2026-05-09)

## Bug fixes

- **add GITHUB_PATH for st-* commands in self-install path (#403)**
Add 'echo "$GITHUB_WORKSPACE/.venv/bin" >> "$GITHUB_PATH"' to the self-install branch of setup/standard-tooling so st-* commands are on PATH for all subsequent steps. Remove per-call-site PATH workarounds from 5 files that are now dead code.


## Chores

- **shorten issue template header comments to fit yamllint line-length**
Remove the word canonical from the source-pointer header to bring both template files under the 80-character yamllint line-length limit. Ref wphillipmoore/standard-tooling#626

- **bump version to 1.5.15**

## Documentation

- **add CI/CD namespace convention design spec (#383)**
- **move spec to docs/specs/ per project convention**
- **add CI/CD namespace convention implementation plan (#383)**
- **add CI YAML standardization design spec (#387)**
- **add st-* command availability normalization spec (#403)**
- **address pushback review for st-* normalization spec (#403)**
Add container WORKDIR explanation to the GITHUB_PATH mechanism section and expand validation to cover Pattern 1 workaround removal. Mark spec ready for implementation.

- **add st-* command normalization implementation plan (#403)**
