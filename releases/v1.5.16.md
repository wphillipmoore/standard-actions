
# Release 1.5.16 (2026-05-09)

## Bug fixes

- **make container-tag a required input in publish-release.yml**
The default of 'latest' was never valid — no language-specific dev container image publishes a :latest tag. Callers must now specify the tag explicitly to match their supported language version. Ref #402

- **make language/container-tag optional, add registry-publish input**
language defaults to base and container-tag defaults to latest so non-language repos can omit them. A new registry-publish boolean input (default false) is added for use in the next commit. Input validation catches invalid combinations: language-specific images with latest tag, and registry-publish without a language. Ref #411, #412

- **gate registry-specific steps behind registry-publish input**
Nine steps that are only meaningful for external registry publication (Maven creds, ecosystem command derivation, build, attestation, SBOM, PyPI publish, registry publish, dist directory, version-dependent inputs) are now gated behind the registry-publish boolean input. Callers must opt in with registry-publish: true. Ref #411, #412

- **use env vars in validation step to avoid expression injection**
Semgrep flagged direct use of inputs.* in run: blocks as an injection risk. Route inputs through env vars instead. Ref #412


## Chores

- **bump version to 1.5.16**

## Documentation

- **add implementation plan for registry-publish flag**
Covers adding a registry-publish boolean input to publish-release.yml and updating all 11 fleet callers. Ref #412

- **add design spec, updated plan, and pushback review for registry-publish flag**
Design spec covers the registry-publish input, input validation, step gating, and fleet caller updates for issues #411 and #412. Plan updated to reflect pushback findings: input validation step added, standard-tooling and ai-research-methodology corrected to not publish. Pushback review documents all findings and resolutions.
