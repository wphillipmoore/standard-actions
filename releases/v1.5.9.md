
# Release 1.5.9 (2026-05-06)

## Bug fixes

- **complete .venv/bin PATH fix for all st-* invocations and add GHAS required checks**
Extend the PATH workaround from #362 to the two remaining composite actions that invoke st-* tools without prepending $GITHUB_WORKSPACE/.venv/bin:

- actions/standards-compliance: st-pr-issue-linkage (fixes #368)
- actions/release-gates/version-divergence: st-version (via inputs)

Also adds Semgrep OSS and Trivy (integration_id 57789) to the CI gates ruleset so GHAS PR-diff-aware alert checks are required before merge (fixes #367).
